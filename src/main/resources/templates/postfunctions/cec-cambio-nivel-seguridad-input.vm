Post function que cambia el nivel de seguridad en caso de que el especialista sea externo a WK.
<table class="aui">
	<tr>
    	<td>
        	Usuario administrador con permisos de edici�n sobre el proyecto CE :
    	</td>
    	<td>
    		&nbsp;&nbsp;&nbsp;&nbsp
        	<select name="userAdmin">
				#foreach ($user in $allUsers)
					<option value="$user.name" 
					#if($!userAdmin && $user.name == $userAdmin)  
						selected="selected"
					#end>$user.name</option>
				#end
			</select>
    	</td>
    	<td width=40%>
    	  &nbsp;&nbsp;&nbsp;&nbsp
    	</td>
	</tr>
</table>